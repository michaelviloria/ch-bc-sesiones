doctype html
html(lang="en")
  - const titlePage = "Home";
  include layout/head.pug
  body
    h1 Hola, #{name} bienvenido al Inicio.
    form(action="/logout", method="post") 
      button(type="submit") Salir